{
  "name": "the-vault-windows",
  "version": "0.9.10",
  "description": "Slay the Spire 2 companion — Windows build",
  "private": true,
  "scripts": {
    "dev":   "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "lint":  "cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}

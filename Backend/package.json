{
  "name": "vault-coop-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker backend for The Vault — co-op lobbies, Steam OpenID, and presence proxy.",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.86.0"
  }
}
